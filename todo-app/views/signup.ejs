<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link rel="stylesheet" href="./css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
  </head>

  <body class="bg-gray-800 text-gray-100">
    <div class="grid grid-cols-6 p-3 gap-2">
      <div class="col-start-3 col-span-2">
        <%- include('header.ejs') %>
        <h6 class="py-4">Signup as a new user</h6>
        <form action="/users" method="post">
            <input type="hidden" name="_csrf"  value="<%= csrfToken %>">
            <div class="py-3">
                <label for="firstName">First name</label>
                <input name="firstName" type="text" class="rounded border-2 border-gray-500 p-2 w-full text-gray-900">
            </div>
            <div class="py-3">
                <label for="lastName">Last name</label>
                <input name="lastName" type="text" class="rounded border-2 border-gray-500 p-2 w-full text-gray-900">
            </div>
            <div class="py-3">
                <label for="email">Email</label>
                <input name="email" type="email" class="rounded border-2 border-gray-500 p-2 w-full text-gray-900">
            </div>
            <div class="py-3">
                <label for="password">Password</label>
                <input name="password" type="password" class="rounded border-2 border-gray-500 p-2 w-full text-gray-900">
            </div>
            <div class="py-3 flex flex-row">
                <button type="submit" class="py-2 px-6 rounded font-medium bg-green-600 hover:bg-green-800">Sign Up</button>
                <p class="py-2 px-4">Already a member? <a href="/login">Sign-in</a></p>
              </div>
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <div class="bg-red-700 mt-4 py-2 px-3 rounded shadow-lg font-mono font-semibold">Error: <%= messages.error[i] %> !!</div>
          <% } %>
        <% } %>
      </div>
    </div>
  </body>